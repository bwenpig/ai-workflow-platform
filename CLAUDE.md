# CLAUDE.md

## ⚠️ 系统环境规则

### Python 命令
- **永远使用 `python3`**，不要用 `python`
- macOS 上 `python` 命令不存在（会触发 Xcode 安装提示）
- 所有 Python 脚本执行统一用 `python3 <script>`

### Shell 脚本
- macOS 默认 shell 是 zsh，不是 bash
- 使用 `zsh` 或 `sh` 执行脚本，避免 `bash` 特有的语法
